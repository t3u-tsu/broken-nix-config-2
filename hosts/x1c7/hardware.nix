# Generated on this machine with:
#   nixos-generate-config --root /mnt --dir /tmp/nixos
# Copy the `fileSystems` / `swapDevices` sections from /tmp/nixos/hardware.nix
# into this file (adjust device paths to stable by-id names).
{ config, lib, ... }:
{
}
